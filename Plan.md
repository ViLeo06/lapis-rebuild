# 《佣兵传说》复刻项目计划

> 版本：v2.1｜更新：2026-09-15｜Web-first  
> 用途：个人怀旧、研究、非商业复刻。优先完整实现剑士与巫师。  
> 工作规则：AGENTS.md；任务：Backlog.md；证据：docs/evidence-ledger.md。

## 0. 当前状态（每个实质检查点更新）

### 0.1 当前检查点

主线为 Vite + TypeScript + Phaser。已有 Godot 诊断工程仅保留参考，不继续双线实现。

- 当前分支：`codex/m0-ani`；Draft PR #2；未合并 main，未公开部署。
- M0/G0：已有可复现静态基线。本轮再次重组并验证原始包及资源。
- M1：核心解析通过，特效、音频、UI 等继续收口，不能称为所有资源和语义全部还原。
- M2：Web 诊断工程已实现，正在完成浏览器验收。
- M3：临时训练场的移动、技能、双木桩战斗与存档已实现；不等同于原版战斗完整复刻。
- 首轮 CI `34933950368`：合成资源、真实资源各 11/12 项浏览器测试通过；唯一失败为 80 个动作/方向组合共用 60 秒限时而超时。已拆成 10 个独立测试并新增完整战斗→结算→存档检查，待本提交重新执行。
- G2-Web 暂不标记通过：必须完成重跑及截图检查，不以构建成功代替浏览器验收。

### 0.2 已验证的基线

| 项目 | 结果 | 范围/证据 |
| --- | --- | --- |
| 安装包 | 470,688,152 字节；18 分卷重组 | SHA-256 见下 |
| 静态展开 | 22,885 文件 / 2,699,237,296 字节 | 未执行安装器、NeoDark.exe、未知 DLL |
| ANI | 6,948 个文件的既有结构记录；本轮复测目标资源 | 1236 字节；8×32 槽，仅使用有效帧前缀 |
| 双职业资源 | 20 阶段×5 动作，100 对 ANI/SPR，0 错误 | `tools/validate/validate_character_assets.py` |
| SGR | 全部 92 文件解析，0 错误 | 后缀按大小写不敏感统计 |
| 地图0 | 1536×768；IMF 47×47；607 个值为1的可通行格 | 已有独立渲染/参考对照；当前 Web 用扁平图 |
| 坐标 | 607 可通行格均满足参考公式往返 | 只在有效偶数奇偶格成立；不声称任意坐标互逆 |
| 输入指纹 | 29 个 Web 用到的真实源文件固定哈希 | `manifests/web-source-baseline.json` |
| 数据 | 剑士/巫师十阶段、各3个技能记录 | `data/classes/`、`data/skills/mvp.json` |
| Web逻辑 | 本地27测试通过；类型检查及 production build 通过 | 扩充版本的 CI 待重跑 |
| Python | 本地10测试通过 | 原解析测试2项+离线包安全测试8项 |
| 浏览器首轮 | 真实地图、动作播放、点击移动、技能耗蓝、IndexedDB、离线HTML已运行通过 | 首轮11/12；完整矩阵和新增闭环待重跑 |

安装包 SHA-256：`c42f37b06f27a6ee0b14e6fea6129cf89956a3e1c7a37c1172a28577f6cdae88`。

精确内嵌 payload SHA-256：`9beb606655d2553c03e80d7eda36a48c135976a3812ce5432d3b2af23e996357`。

### 0.3 尚未完成

- 浏览器完整组合矩阵及完整训练闭环的最新回归验收。
- 30分钟真实连续运行测试，不能以加速模拟或单元测试冒充。
- 真实 MagicRes/FOCUS 特效、音效、原版死亡表现和遮挡层。
- 真正的地图切换、背包装备、升级、任务等完整系统。
- 原版时序、行动机制、技能效果和路径偏好等行为校准。
- 公开预览访问控制及版权审查；没有因仓库私有就假定网页/静态资源也私有。

### 0.4 下一项动作

完成本提交的合成/真实资源浏览器 CI → 下载报告/截图验收 → 修复实际失败 → 更新 G2 状态 → 在同一条 Web 主线上继续 M3 的可验证功能。Windows VM 缺失不阻塞转换、Web、测试或数据工作。

## 1. 目标与范围

### 1.1 实施路线

证据固化 → 资源转换 → Web诊断原型 → Web可玩切片 → 行为校准 → 核心系统 → 双职业完整化 → Web发布 → 可选联网。

终端用户打开现代桌面浏览器即可体验，不安装原 Windows 客户端，不配置 VM、Node 或 Python。开发者才需要构建工具。私人独立 HTML 可作为免安装检查点；正式发布以静态 Web 构建为基础。

### 1.2 MVP验收目标

1. 浏览器启动、角色选择（剑士/巫师）。
2. 一张安全/测试地图与一张战斗场景或有明确边界的战斗区域。
3. 移动、方向、待机、攻击、受击、施法、死亡表现；未考据项明确标注。
4. 每职业普通攻击及至少3代表技能，包含敌人、伤害、死亡和结算。
5. NPC对话、地图切换、基础背包和装备。
6. IndexedDB本地存档/读档；JSON导出/导入作为可迁移备份。
7. Debug Panel 可检查角色、动作、方向、帧、锚点、地图坐标及通行数据。
8. 可重复构建、可追溯资源清单、浏览器实际测试、30分钟稳定性检查。

仅有按钮、临时木桩和占位效果，不足以宣布 MVP 全部完成。

### 1.3 V1验收目标

剑士 B100–B190、巫师 B109–B199 各十阶段，完整成长到最高阶段：外形、动作、属性、主要技能树、装备适配、升级、任务、地图、音频、设置和存档恢复。按职业矩阵逐项验收，不以图片齐全代替职业完整。

### 1.4 暂缓范围

第三职业、大规模地图、全部原作内容；公网账号/充值/交易/运营后台；第三方服务器互通；原生移动App和主机；Windows优先发行；未经授权公开散布原资源。PWA/桌面封装仅为以后可选方式。

## 2. 技术与资产边界

- 主运行时：Phaser + TypeScript；Vite开发/构建；原生HTML/CSS调试面板，不预先引入React。
- Python >=3.12：静态提取、SPR/ANI/SGR/MMF/SMF/IMF/Set.lib解析、资源转换、校验。
- 浏览器消费 PNG/JSON/适配后的音频，不执行原始EXE/DLL，不在前端放密钥。
- 当前锁定：Phaser3.90.0、Vite7.3.6、TypeScript5.9.3、Playwright1.63.0；以 package-lock.json 为准。
- GitHub：唯一代码仓库 `ViLeo06/lapis-rebuild`（私有），存代码、文档、工具、数据、清单、测试与合成样本。
- Drive：`lapis-rebuild-assets`，存原包、完整解包、批量转换和大预览。稳定目录ID以 `manifests/storage-locations.json` 为准。
- `web/public/game-data/`、`game/generated/`、原包、node_modules、测试截图和大文件不进Git。
- 私有原版预览与公开合成测试分开；第三方依赖许可随打包保留；不得分发字体文件。
- 改动先工作分支/PR，禁止直接更新或擅自合并 main。

## 3. 证据与临时实现

`VERIFIED`：直接文件、哈希、严格解析或可重复实验。必须注明验证对象/版本，不将兼容运行时的描述扩大为2003国服事实。

`INFERRED`：动作名、方向名、兼容代码代表旧行为等推断，保留来源与不确定性。

`UNVERIFIED`：用于推进原型的设计替代。允许自主实现可逆、局部、明确标记的训练参数，不等于授权改变正式玩法或公开发布。

- 原版语义和临时规则分离。时序、移速、攻击伤害、Buff、敌人、奖励集中于 `web/src/config.ts` 等可替换策略。
- ANI raw timing 原值保留，不自动认定毫秒/FPS；`_05` 不统一当死亡；FOCUS 不盲套 Body_ 八方向语义。
- 锚点使用 SPR bounds，不按各帧中心导致人物抖动。
- 地图点击函数与格坐标锚点函数分开；偶数奇偶格的往返性质不推广到所有像素。
- 原版 behavior 校准需隔离 Windows VM；不能以“继续”为由在常用机器或普通 CI 执行未知客户端。
- 无法确认的正式玩法替代需列选项，请负责人决策；可继续处理其他不依赖该决定的工作。

## 4. 里程碑与门禁

| 里程碑 | 交付与验收 | 状态 |
| --- | --- | --- |
| M0 基线 | 固定样本、哈希、静态拆包、可复现工具 | G0通过；大镜像归档尾项独立追踪 |
| M1 资源 | 双职业、地图、技能数据、标准资源与可追溯索引 | 核心通过；特效/音频/UI收口中 |
| M2 Web诊断 | 浏览器地图0+B100/B109；动作方向逐帧；调试面板；CI | 实现完成，最终浏览器验收中 |
| M3 可玩切片 | 移动→战斗→结算→NPC/地图→存档；完整闭环；30分钟稳定 | 训练子集已实现，未宣布完整通过 |
| M4 行为校准 | 行动、时序、伤害、输入/UI；录像/静态证据/回归测试 | 静态线索持续积累，动态待隔离VM |
| M5 核心系统 | 实体、战斗、技能、地图、成长、背包装备、任务、存档迁移 | 未完成 |
| M6 双职业完整化 | 十阶段职业矩阵逐项通过或明确批准例外 | 未开始 |
| M7 内容与Web发布 | 地图任务流程、性能、兼容、访问控制、版权、可回滚构建 | 未开始 |
| M8 可选联网 | 单机V1稳定后，独立权威服务端，私有邀请优先 | 暂缓，另行审批 |

- G0：基线可复现；G1：目标资源和至少一张地图稳定转换。
- G2-Web：浏览器真实运行、完整诊断组合和异常处理验证；未通过前不大规模扩展玩法。
- G3：Web可玩切片完整验收和30分钟稳定性；通过前不扩第三职业及大地图内容。
- G4：关键行为有证据或被明确接受的正式替代。
- G5：双职业矩阵完成或批准例外；未通过不称V1。
- G6：Web发布/访问控制/版权/构建检查通过，并取得发布授权。

## 5. 分阶段具体任务

### M0/M1持续收口

维护客户端分析、证据台账、安装包及输入/输出指纹；解析器损坏输入显式报错，不静默修补。100目标ANI/SPR、92SGR、Set.lib成员、地图0像素对比持续回归。完成MagicRes/FOCUS、音频/UI/字体授权、文本编码及资源索引。

完整解包镜像未上传时可用原分卷+完整哈希+工具复现，必须写“镜像未归档”，不得谎称同步完成；不得因一次上传失败反复重建相同文件。

### M2 Web诊断

加载地图0；显示/选择B100与B109；原始槽00/01/02/03/05；8方向槽；暂停、单步、跳帧、临时帧时长；显示原始timing、SPR索引及bounds。地图缩放、tile/resource检查、原始IMF、通行投影与坐标显示。缺失/错误资源关闭功能并提示，不能悄悄用占位资源冒充原版。

### M3 Web可玩切片

复用诊断底座，建立角色与敌人状态机、目标/范围/伤害/MP/冷却、每职业3技能；任务说明或NPC、战斗切入/退出、结算、地图切换、背包装备、存档恢复。临时木桩和圈形效果必须明示。采用WASD/点击开发控制不代表原版输入已恢复。

### M4行为校准

隔离VM快照，最小文件/账号权限，不用真实密码。采集移动、镜头、碰撞、攻击/施法前后摇、命中、死亡、行动推进、升级、装备、NPC/商店/地图切换。网络只被动观察授权样本，不绕过认证或攻击第三方服务。

### M5/M6系统与职业

核心模块数据驱动；技能/装备条件、效果和事件与表现解耦。十阶段逐项检查外形、动作、武器、属性、技能学习、升级条件、MP/伤害、存档恢复。标注未知/缺失，不悄悄替换。

### M7/M8发布与联网

先稳定单机Web流程，验证浏览器、常见分辨率、高DPI、首次加载、资源丢失、存档迁移和回滚。构建带版本、提交、资源版本、测试摘要与SHA-256。访问页面与资产都需受控。联网单列账号、安全、成本、服务端权威和隐私审批，不复用未知第三方服务器。

## 6. 质量与自动化

- `python3 -m unittest discover -s tests/parsers -v`。
- Web：`npm ci --ignore-scripts`、`npm run typecheck`、`npm test`、`npm run build`、`npm run test:e2e`。
- 合成样本 CI 不需私有素材；原版 smoke 仅可信工作分支显式标记触发，先哈希后静态提取，凭据不落盘、不运行原包。
- E2E检查真实画布/动画矩阵/移动/技能/存档/异常/响应式/离线零外网请求；保留精简JSON报告和截图。
- 浏览器测试失败要修问题或合理拆分测试，不能删除覆盖项或伪造passed。
- 真实30分钟持续运行与加速逻辑测试分别记录，不混用。
- IndexedDB可能受浏览器配置影响；JSON备份保留，存档必须校验schema、资源包标识与数据范围，错误时不修改当前状态。
- 公开部署、main合并、付费、权限修改、原客户端动态运行仍单独确认。

## 7. 目录与文档职责

`AGENTS.md` 控制执行行为；`Plan.md` 控制范围/里程碑/当前状态；`Backlog.md` 列具体任务；`docs/evidence-ledger.md` 记录证据；`docs/decisions/` 保存架构决策；`tools/` 解析与转换；`data/` 结构化规则；`web/` 当前主运行时；`game/` 历史Godot参考；`manifests/` 可追溯指纹；`.github/workflows/` 自动验证。

## 8. 进度维护规则

1. 每个实质检查点在同一提交或PR序列更新本文件0节和对应Backlog状态。
2. 记录日期、已完成项、实际测试命令/结果/提交、下一动作、阻塞与未验证事项。
3. 本地成功、CI成功、浏览器成功、资源完整和原版还原度分别报告；不填无依据百分比。
4. 首次读当前分支、最新Plan/AGENTS/Backlog与文件，不把旧聊天当工作区状态。
5. 不重排或覆盖他人未确认改动；ref更新必须非force快进；不自动合并main。
6. 安全、低风险、可逆的工程任务持续推进，不因Windows VM未就绪停Web工作。
7. 真正阻塞时列出缺少的输入/能力、已尝试方法、可继续的独立任务和最小人工动作。
8. “持续推进”指当前实际执行及明确启动的CI任务；普通对话结束后不得声称仍在后台开发。

## 9. 变更记录

- 2026-09-14 v1.1：M0基线与Windows/Godot路线。
- 2026-09-15 v2.0：用户批准Web-first与AGENTS控制，Windows VM只用于行为考据。
- 2026-09-15 v2.1：Web诊断/训练代码、锁文件、合成与原版CI、免安装HTML、输入哈希和存档；记录首轮11/12失败项，拆分后等待重验，不提前关闭G2。
