# 《佣兵传说》复刻项目计划

> 文档版本：v2.0  
> 更新日期：2026-09-15  
> 项目定位：个人怀旧、研究与非商业复刻  
> 第一优先级职业：剑士、巫师  
> 当前主路线：**Web-first，浏览器直接运行与调试**

## 0. 当前状态（持续维护）

本节是项目的实时状态面板。任何会改变项目实际进度、技术路线、门禁结论或主要阻塞项的提交，都必须在同一变更中同步更新本节。

### 0.1 当前结论

项目已从“Godot + Windows 离线客户端优先”正式切换为 **Web-first**：

- 玩家和开发验收优先通过浏览器完成，不要求安装 Windows 客户端或配置旧版运行环境。
- 运行时主技术栈改为 **Vite + TypeScript + Phaser**；HTML/CSS 用于调试面板和辅助界面。
- Python 3.12 继续承担原始客户端资源拆包、解析、转换和验证。
- 原始 Windows 客户端只作为“行为考据样本”；需要动态运行时，必须放在隔离的一次性 Windows VM 中。
- Windows VM **不再阻塞** Web 原型和可玩切片开发。无法确认的参数先以 `UNVERIFIED` 标记实现，后续用证据校准。
- 现有 Godot 原型保留为历史验证/参考实现，不再作为默认运行时主线；除非新的架构决策重新启用。

### 0.2 已完成/已验证

- [x] M0 / G0：2.2 客户端基线已重新验证，可静态复现。
- [x] 安装包大小：`470,688,152` 字节。
- [x] 安装包 SHA-256：`c42f37b06f27a6ee0b14e6fea6129cf89956a3e1c7a37c1172a28577f6cdae88`。
- [x] 精确内嵌 7z payload SHA-256：`9beb606655d2553c03e80d7eda36a48c135976a3812ce5432d3b2af23e996357`。
- [x] 静态解包：22,885 个普通文件，约 2.699 GB。
- [x] `.spr` 严格解析器：RGB565 行游程、透明区域和帧边界已验证。
- [x] `.ani` 固定布局已验证；6,948 个文件均为 1,236 字节；只使用每方向前 `frames_per_direction` 个有效槽位。
- [x] 剑士/巫师 20 个职业阶段、100 组目标 ANI/SPR 配对全部通过严格解析与索引范围验证。
- [x] `.sgr`：92/92 文件可解析。
- [x] 地图 0“对练场”独立渲染与兼容运行时参考图逐像素一致。
- [x] `Set.lib` 目录、加密和 PKWARE DCL 解压链已恢复；12 个成员可提取。
- [x] 已结构化剑士、巫师职业基础数据及 MVP 技能样本。
- [x] 已建立可生成角色动作帧、地图 PNG、碰撞数据和原型清单的 Python 转换链。
- [x] 已建立 Godot 诊断原型骨架，但不再作为默认主线。

### 0.3 当前未完成/待验证

- [ ] 建立正式 Web 工程：Vite + TypeScript + Phaser。
- [ ] 浏览器显示地图 0、B100、B109，并可切换动作/方向/帧。
- [ ] 建立 Web Debug Panel：角色、动作、方向、帧、地图格、阻挡、资源 ID、坐标信息。
- [ ] 建立 GitHub Actions：Python 解析回归 + Web build/test。
- [ ] `MagicRes` 的 `FOCUS` 动画语义尚未完全证实；不能直接套用 `Body_` 的八方向索引解释。
- [ ] ANI 原始 timing 字段单位尚未证实；Web 实现必须保留原值并将临时播放速度标记为 `UNVERIFIED`。
- [ ] 地图移动坐标转换仍需校准；`screen_to_cell` 与 `cell_to_screen` 不能假设为直接互逆。
- [ ] 音频、字体、UI、文本和更多数据表仍需完成系统盘点与转换。
- [ ] 原版移动、攻击、施法、命中、死亡、UI 等行为仍需后续在隔离 Windows VM 中采集证据。

### 0.4 当前下一目标

**M2：Web Diagnostic Prototype**

验收目标：打开浏览器即可看到“对练场”及剑士/巫师原始角色资源，并通过调试面板检查动画、方向、帧、地图坐标和通行数据；整个过程不依赖原 Windows 客户端。

---

## 1. 实施路线

项目采用：

**证据固化 → 资源格式还原 → Web 诊断原型 → Web 可玩切片 → 原版行为校准 → 核心系统 → 剑士/巫师完整化 → 内容发布 → 可选联网**

原则：先形成浏览器内可见、可操作、可验证的闭环，再逐步提高“像原版”的精度；不因原版客户端动态考据暂时不可用而阻塞开发。

### 1.1 技术基线

- Web 游戏运行时：**Phaser + TypeScript**。
- 开发/构建：**Vite**。
- 调试与辅助 UI：优先原生 HTML/CSS；只有复杂度明显上升时才考虑 React 等框架。
- 资源解析：Python 3.12。
- 运行时资源：PNG、JSON、OGG/WAV 等浏览器友好格式。
- 数据层：JSON 为主要开发与运行时结构化格式；原始编号、来源与证据链必须保留。
- 自动化：GitHub Actions 执行 Python 测试、资源结构验证、TypeScript 检查和 Web build。
- 发布：优先提供受控的浏览器预览；公开部署前单独检查版权和资源授权边界。
- Windows VM：仅用于原客户端动态行为考据，不作为玩家环境和日常开发环境。

### 1.2 存储与协作基线

- 唯一代码仓库：`https://github.com/ViLeo06/lapis-rebuild`（私有）。
- 大文件归档：Google Drive 私有目录 `lapis-rebuild-assets`。
- GitHub 保存代码、文档、解析工具、结构化数据、测试、清单和少量授权/合成黄金样本。
- Google Drive 保存原始安装包、内层资源包、完整解压归档、批量派生产物和大体积预览。
- GitHub 清单通过稳定的 Drive 文件夹 ID、相对路径、来源、版本、大小和 SHA-256 关联归档；不得记录密码、令牌或临时下载地址。
- 不直接向 `main` 提交；所有工程化变更经功能分支和 Pull Request。
- 当前活跃工程分支为 `codex/m0-ani`，对应 PR 在合并前持续作为本阶段集成分支使用。

---

## 2. 项目目标

### 2.1 最小可玩版本（MVP）

MVP 必须形成一个可验证的浏览器体验：

1. 使用现代桌面浏览器打开页面即可进入游戏，不要求安装 Windows 客户端。
2. 可选择剑士或巫师进入游戏。
3. 至少包含一张安全/测试地图和一张战斗场景或等价的战斗区域。
4. 支持原版风格的角色朝向、待机、移动、攻击、受击、施法和死亡表现。
5. 剑士和巫师各实现普通攻击及至少 3 个有代表性的技能。
6. 支持敌人、伤害、死亡、掉落或战斗结算。
7. 支持 NPC 对话、地图切换、基础背包和装备。
8. 支持浏览器本地存档和读档；首选 IndexedDB，必要时可用 LocalStorage 保存轻量配置。
9. 提供开发者 Debug Panel，可查看关键状态、坐标、资源 ID、动画和碰撞/通行信息。
10. 形成可重复构建的静态 Web 产物；在受控环境中可直接部署预览。

### 2.2 第一正式版本（V1）

在 MVP 基础上，V1 重点完成：

- 剑士 `B100–B190` 十阶段外形及对应动作资源。
- 巫师 `B109–B199` 十阶段外形及对应动作资源。
- 两条职业路线的等级、HP、MP、移动、攻击范围、职业系别和技能数据。
- 两个职业的主要技能树、装备适配和升级体验。
- 一组能体现原版循环的城镇、野外、战斗/副本地图与任务。
- 设置、音频、存档、异常恢复和主流桌面浏览器兼容性。
- 可选 PWA/桌面封装只作为后续发行方式，不改变 Web-first 核心。

“完整职业”以能够从初始阶段正常成长到最高阶段为验收口径，不只要求立绘或动作资源齐全。

### 2.3 暂不纳入首版

- 原版全部职业、地图、任务和怪物。
- 公网账号体系、充值、交易和运营后台。
- 与非授权原服务端或第三方服务器互通。
- 原生移动 App 和主机平台。
- 以 Windows 原生程序作为唯一或主要发行形式。
- 公开分发原客户端或未经许可的原始美术、音乐资源。

---

## 3. 证据与实现规则

### 3.1 证据等级

所有关键结论必须归入以下之一：

- **VERIFIED**：由文件格式、哈希、严格解析、逐像素对比、静态源码/二进制证据或可重复实验直接支持。
- **INFERRED**：证据较强但仍包含语义推断，例如动作槽含义、方向名称。
- **UNVERIFIED**：为推进 Web 原型而采用的临时参数或设计替代，例如动画毫秒数、尚未考据的移动速度。

不得把 `INFERRED` 或 `UNVERIFIED` 写成已验证的原版事实。

### 3.2 Web 原型不等待动态考据

- 缺少动态行为证据时，允许使用临时参数继续实现。
- 临时参数必须集中管理并标记 `UNVERIFIED`，不得散落为无注释硬编码。
- 后续获得 Windows VM 行为证据后，以校准数据替换临时值并增加回归测试。
- 动态考据是“提高还原精度”的阶段，不是“开始做游戏”的前置条件。

---

## 4. 里程碑总览

| 里程碑 | 目标 | 主要交付物 | 状态 |
| --- | --- | --- | --- |
| M0 基线归档 | 样本、哈希、工具和结论可复现 | 拆包工具、哈希清单、客户端分析 | **完成 / G0 通过** |
| M1 资源解码 | 目标职业、地图、数据可稳定转换 | SPR/ANI/SGR/Set.lib 工具、结构化数据 | **核心通过 / 收口中** |
| M2 Web 诊断原型 | 浏览器正确显示地图和角色并可检查底层状态 | Vite/Phaser 工程、Debug Panel、地图0、B100/B109 | **下一阶段** |
| M3 Web 可玩切片 | 一张地图形成移动与基础战斗闭环 | 浏览器 Demo、两职业基础角色、敌人、技能 | 未开始 |
| M4 行为校准 | 用原客户端证据校准操作、战斗、UI 与时序 | 行为录像、状态表、校准数据、回归测试 | 未开始，可与 M3 并行 |
| M5 核心系统 | 成长、技能、装备、NPC、存档工程化 | 数据驱动核心模块及自动化测试 | 未开始 |
| M6 双职业完整化 | 剑士、巫师可完整成长和战斗 | 十阶段资源、技能树、职业验收矩阵 | 未开始 |
| M7 内容与 Web 发布 | 形成稳定的浏览器怀旧版本 | 地图/任务样板、静态 Web 构建、说明 | 未开始 |
| M8 可选联网 | 在单机 Web 版稳定后增加多人能力 | 独立服务端、账号与同步协议 | 暂缓 |

时间估算只用于排期，不作为承诺。实际节奏以门禁和可验证产出为准。

---

## 5. 分阶段执行计划

### M0：项目基线与证据固化

状态：**完成，G0 通过。**

核心成果：

- 2.2 安装包来源、大小、SHA-256 已记录。
- 已静态定位并精确切割内嵌 7z payload。
- 已形成可重复拆包和文件清单流程。
- 已建立 `docs/client-analysis.md`、解析工具和哈希清单。
- 原始客户端无需执行即可完成主要静态提取。

保留尾项：完整 2.7 GB 解压镜像是否同步至 Drive 不阻塞后续工程，因为原始分片、哈希和脚本可复现。

### M1：资源格式与数据层还原

状态：**核心通过，继续收口。**

已完成：

1. `.spr` 严格解码。
2. `.ani` 固定布局和 active slot 规则。
3. 剑士/巫师 100 对 ANI/SPR 全量验证。
4. `.sgr` 92/92 解析。
5. MMF/SMF/IMF 基础解析及地图 0 独立渲染。
6. `Set.lib` 解密、DCL 解压及 12 个成员提取。
7. 剑士/巫师职业数据和首批代表技能结构化。
8. 原型资源生成脚本。

继续完成：

- `MagicRes/FOCUS` 语义。
- 音频、字体、UI、文本盘点与标准化转换。
- 资源索引和可追溯清单收口。
- 地图移动坐标和碰撞语义继续验证。

M1 收口标准：所有 Web MVP 所需资源均能由脚本确定性生成，未知语义有明确标签而非静默猜测。

### M2：Web Diagnostic Prototype

状态：**立即执行。**

技术：Vite + TypeScript + Phaser。

第一版必须做到：

- 浏览器加载地图 0“对练场”。
- 显示 B100 剑士和 B109 巫师。
- 可播放原始动作槽 `_00/_01/_02/_03/_05`。
- 可切换原始方向槽和逐帧查看。
- 使用 SPR bounds 锚定角色，避免帧尺寸变化造成视觉抖动。
- 显示 Debug Panel：
  - character/resource ID
  - action slot
  - direction slot
  - current frame / frame count
  - raw ANI timing
  - map pixel coordinate
  - logical cell coordinate（若转换已验证）
  - IMF/collision raw value
  - walkable 判定
  - hovered resource/tile ID（能可靠获取时）
- 可开关网格、碰撞/通行覆盖层、角色 anchor/bounds。
- 对所有临时帧时长、方向语义和动作名称显示 `INFERRED`/`UNVERIFIED` 标记。

验收标准：在不运行原客户端的情况下，通过浏览器即可检查地图、角色动画、帧索引和基础地图数据。

### M3：Web 可玩切片

目标：形成最短可玩闭环。

任务：

- 角色选择：剑士 / 巫师。
- 地图渲染、镜头、碰撞和开发期移动控制。
- 角色待机、移动、攻击、受击、施法、死亡状态机。
- 一个近战敌人和一个远程/施法敌人。
- 普通攻击、目标、范围、伤害、MP 消耗。
- 每职业接入 3 个代表技能。
- 一名 NPC、一段对话、一次地图切换或战斗结算。
- 浏览器本地存档/读档。

在原版行为尚未校准时，允许开发控制使用 WASD/键盘和明确标注的临时参数；最终原版风格控制方式在 M4 校准。

验收标准：从浏览器打开页面到完成一次战斗并保存进度，流程不依赖手工修改文件。

### M4：原版行为校准

动态运行原客户端只允许在一次性 Windows VM/快照环境中进行；不使用常用系统、真实账号或真实密码。

需要校准：

- 分辨率、坐标系、镜头、地图滚动、UI 布局和输入方式。
- 行走路径、转向、移动速度、碰撞、攻击范围和施法距离。
- 普通攻击、技能前摇/命中/后摇、伤害显示、受击和死亡。
- 行动推进/回合机制。
- 职业升级、转职、装备、属性和技能学习。
- NPC、商店、任务、地图切换和战斗流程。

网络请求只做被动观察和协议边界识别，不尝试绕过认证、攻击服务器或操作第三方账号。

交付物：`docs/behavior-spec/`。每项关键规则附录像时间点、截图、表格或静态证据；无法考据时保留明确的设计替代。

### M5：核心系统工程化

- 实体与属性：角色、怪物、NPC、装备、Buff/Debuff。
- 战斗：目标、范围、命中、伤害、治疗、状态、死亡、经验和掉落。
- 技能：数据驱动的条件、消耗、范围、效果和动画事件。
- 地图：图块、碰撞、寻路、传送、事件和刷新点。
- 成长：等级、职业阶段、技能学习、装备限制和属性成长。
- 内容：对话、商店、任务、奖励和条件。
- 系统：输入、设置、音频、浏览器存档迁移、日志和错误报告。

关键约束：职业和技能规则不得散落为大量硬编码；同一系统应能通过数据扩展到其他职业。

### M6：剑士与巫师完整化

剑士：

- 十阶段外形、动作、武器与装备表现。
- 近战距离、移动、单体/范围攻击和防御能力。
- 阶段成长、技能解锁和战斗差异。

巫师：

- 十阶段外形、动作、法术特效和施法锚点。
- MP、施法距离、范围、弹道/落点和状态效果。
- 阶段成长、技能解锁和战斗定位。

验收使用“职业矩阵”：每阶段检查外观、动作、属性、装备、技能、伤害/消耗、升级和存档恢复。缺失项必须标记“待考据”或“设计替代”，不得悄悄用相似资源替代。

### M7：内容、质量与 Web 发布

- 选择能代表原版体验的城镇、野外和战斗/副本地图。
- 完成接任务、探索、战斗、奖励的短流程。
- 补齐 HUD、背包、装备、技能、对话、设置和存档界面。
- 优化资源加载、内存、寻路和特效性能。
- 测试主流桌面浏览器、常见分辨率、高 DPI 和中文环境。
- 生成版本号、更新日志、已知问题、操作说明和可回滚静态构建。

公开发布门禁：

- 游戏流程通过。
- 高优先级缺陷清零。
- 原始版权资源不进入未经授权的公开分发渠道。
- 若使用原版资源做在线预览，必须确保访问范围和授权边界符合项目要求。

### M8：可选联网

只有单机 Web V1 稳定后才启动：

- 设计独立权威服务端，不依赖未知第三方服务。
- 先做局域网/私有邀请制，再评估公网部署。
- 服务端负责账号、角色、地图状态、战斗判定和持久化。
- 客户端只提交意图，重要数值由服务端验证。
- 单独评估隐私、安全、成本和运营边界。

---

## 6. 目录结构目标

```text
lapis-rebuild/
├── AGENTS.md                 # Agent/自动化项目控制规则
├── Plan.md                   # 路线、门禁、实时状态
├── README.md
├── Backlog.md
├── docs/
│   ├── client-analysis.md
│   ├── evidence-ledger.md
│   ├── behavior-spec/
│   └── decisions/
├── manifests/
├── tools/
│   ├── extract/
│   ├── convert/
│   └── validate/
├── data/
│   ├── classes/
│   ├── skills/
│   ├── maps/
│   └── items/
├── web/                      # 主运行时：Vite + TypeScript + Phaser
│   ├── public/
│   │   └── game-data/        # 由转换工具生成；大体积内容默认不入 Git
│   └── src/
│       ├── game/
│       ├── debug/
│       └── ui/
├── game/                     # 既有 Godot 诊断原型，默认仅保留作参考
├── tests/
│   ├── parsers/
│   ├── gameplay/
│   ├── web/
│   └── golden/
└── .github/
    └── workflows/
```

原始和大体积派生资源默认仅保存在私有 Drive 或本地生成目录，不进入 Git 历史。

---

## 7. 验证与质量策略

### 7.1 Python/资源层

- 每种格式保留少量合成或可合法纳入仓库的黄金样本。
- 比较帧数、尺寸、bounds、索引、像素哈希和异常处理。
- 目标剑士/巫师 100 对 ANI/SPR 持续回归。
- 92 个 SGR 持续做全量解析回归。
- Set.lib 提取结果用已知成员哈希/大小校验。
- 地图 0 在具备参考图的环境中继续做像素级对比。

### 7.2 Web 层

- TypeScript 类型检查必须通过。
- Web production build 必须通过。
- 关键数据加载失败必须显式报错，禁止静默回退到错误资源。
- 动画/坐标/碰撞逻辑尽量建立确定性单元测试。
- Debug Panel 是研发基础设施，不应在早期为“界面美观”被删减。

### 7.3 CI

GitHub Actions 至少执行：

1. Python parser tests。
2. 结构化数据 schema/一致性检查。
3. Web install/typecheck/test/build。
4. 生成精简验证报告或 artifact。

CI 不运行未知安装器、原始 NeoDark.exe、兼容注入组件或未知 DLL。

---

## 8. 风险与边界

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 原版资源版权不明 | 无法公开发布/商业化 | 代码与版权资源分离；默认私人研究；公开版本需重新确认授权或替换素材 |
| 原客户端/兼容运行时不可信 | 系统和账号安全 | 日常只做静态分析；动态运行仅在隔离 Windows VM/快照中 |
| 服务端逻辑缺失 | 无法准确恢复部分玩法 | 静态数据 + 行为录像交叉验证；缺失规则标记设计替代 |
| `MagicRes/FOCUS` 语义未完 | 技能特效可能误读 | 保留 raw 数据；先做诊断播放器，证据充分后才固化语义 |
| ANI timing 单位未知 | 动画节奏可能不准 | 保存 raw timing；临时毫秒值标记 `UNVERIFIED` |
| 地图坐标转换语义未完 | 移动/点击定位偏差 | 不假设两个转换函数互逆；用证据和可视化调试校准 |
| Web 资源体积 | 首屏加载慢 | atlas/压缩/按需加载；原型先正确后优化 |
| 浏览器差异 | 输入、音频或渲染不一致 | 固定支持矩阵并用 CI/手工冒烟验证 |
| 范围持续膨胀 | 长期没有可玩版本 | M2/M3 门禁前不扩展第三职业和公网服务端 |
| 记忆与实际规则不一致 | 复刻成“印象版” | 关键规则进入 evidence ledger，区分 VERIFIED/INFERRED/UNVERIFIED |

---

## 9. 决策门禁

- **G0：基线可复现。已通过。**
- **G1：目标职业资源与一张地图可稳定转换。核心条件已通过；M1 继续收口非阻塞项。**
- **G2-Web：浏览器诊断原型通过。** 未通过前不大规模实现玩法系统。
- **G3：Web 可玩切片通过。** 未通过前不扩展第三职业和大规模地图内容。
- **G4：关键行为完成证据校准或有明确接受的替代设计。**
- **G5：剑士、巫师职业矩阵完成或明确标记例外。** 未通过前不称为 V1。
- **G6：Web 发布、版权边界和构建检查通过。** 之后才生成对外公开交付物。

---

## 10. 当前冲刺：Web-first 切换

### Sprint A：Web 诊断底座

1. 创建 `web/`：Vite + TypeScript + Phaser。
2. 连接现有 Python 生成资源，不重复解析原格式。
3. 显示地图 0。
4. 显示 B100 / B109。
5. 动作槽、方向、帧播放器。
6. 建立 Debug Panel。
7. 加入网格/通行/anchor/bounds 可视化。
8. GitHub Actions 执行 Python + Web build 验证。

完成标准：浏览器打开即能进行角色和地图资源诊断。

### Sprint B：最短可玩闭环

1. 加入开发期移动控制和碰撞。
2. 加入普通攻击状态切换。
3. 各接入一个代表技能，再扩展到每职业 3 个。
4. 加入最小敌人/伤害/死亡逻辑。
5. 加入本地存档。
6. 把所有未考据参数集中列入待校准数据。

完成标准：浏览器内完成一次可重复的移动和战斗流程。

---

## 11. 计划维护规则

1. **Plan.md 是项目路线和状态的单一主计划。**
2. 每个会改变项目实际状态的工作完成后，在同一个 PR/提交序列中更新 `0. 当前状态` 和对应里程碑状态。
3. 状态只允许使用可验证描述，例如“已通过 100/100 配对验证”，避免没有依据的百分比。
4. 新的事实证据先进入 `docs/evidence-ledger.md`，再据此修改计划或实现语义。
5. 重大架构/范围决策写入 `docs/decisions/`；Web-first 切换属于此类决策，应保留记录。
6. 具体任务拆入 `Backlog.md`；Backlog 不得绕过本文件门禁。
7. 每次状态更新至少记录：完成项、当前下一目标、阻塞项/待验证项、更新日期。
8. 原版行为未知时允许继续工程实现，但必须显式标记 `INFERRED` 或 `UNVERIFIED`。
9. 项目默认持续向前推进，直到遇到以下真实阻塞：需要用户授权的公开发布/合并、需要不安全执行原客户端、缺少关键输入文件、或必须由用户决定的玩法替代方案。
10. 不因 Windows VM 尚未准备好而停止 Web 原型、转换工具、测试、CI 和数据层工作。
