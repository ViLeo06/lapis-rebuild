# 《佣兵传说》复刻项目计划

> 版本：v2.5｜更新：2026-09-15｜Web-first  
> 用途：个人怀旧、研究、非商业复刻。第一优先级：剑士、巫师。  
> 执行规则：`AGENTS.md`；任务：`Backlog.md`；证据：`docs/evidence-ledger.md`。

## 0. 当前状态

### 0.1 当前检查点

主线保持 **Vite + TypeScript + Phaser + Python 静态转换工具链**。Godot 工程只保留参考，不继续双线开发。

- `main` 已合并 PR #2（Web-first/G2/M3 基线）和 PR #3（2.2 安装包归档登记）；未经明确批准不继续合并 `main`。
- 当前人工校准分支：`codex/battle-mode-fix`。本轮目标是修正正常画面诊断叠层，并把错误的“场景内实时训练”模型改回原作有历史证据支持的 **非战斗地图 → 战斗画面 → 行动槽战术循环**。
- M0/G0：完成。安装包、精确 7z payload、全量静态展开、哈希和主要解析器均可复现。
- M1：核心通过。ANI/SPR/SGR/MMF/SMF/IMF/Set.lib/Quest.lib/Tip 已形成静态解析链；音频、UI、字体等继续收口。
- M2/G2-Web：工程门禁通过；第一轮人工视觉验收发现并修复 SPR 横向切片和角色左右倒走，用户已复测确认两项修复正确。
- M3：此前“训练木桩在普通地图上实时打”的结构被用户指出与原作不符。2003 同期资料随后交叉确认：原作有独立战斗画面/战斗状态，参战角色需等 **行动槽** 蓄满后再移动/攻击，之后重新等待；新版 Web 原型已经按该结构拆分 field/battle 状态。
- 第二轮人工反馈同时确认：正常移动不需要路线指引线；角色 SPR bounds/anchor 方框不应持续闪烁。路线线条已从正常渲染移除，bounds 默认隐藏，仅 Debug Panel 手动开启。
- **30 分钟真实墙钟 Web soak 已完成并通过**：run `34969057806`。这是旧检查点稳定性证据，不代表新版战斗模型已经通过新的人工/真实资源验收。

### 0.2 人工验证发现与修复

第一轮真实离线 HTML 人工验证发现：人物水平切片错位、移动朝向左右镜像。两项均已完成根因修复，并由用户复测确认。

**SPR 解码修复**

SPR row span 首个 `uint16` 不是绝对 x，而是从上一 opaque run 末端计算的 **transparent skip**：

`x = previous_run_end + transparent_skip`

真实 B100/B109 多 span 行和修复后的完整人物轮廓共同验证该解释。修复后 fixed-hash 2.2 private-original + synthetic CI `34971954768` 成功。

**ANI 方向修复**

真实 B100/B109 `Body_` walk raw row：

`0 S, 1 SW, 2 W, 3 NW, 4 N, 5 NE, 6 E, 7 SE`

Web east/west 映射已修正，并补八方向回归。

第二轮用户确认上述两项已正确，随后提出三项新校准：正常画面去路线指引线、默认隐藏角色 bounds 方框、恢复战棋/半回合制的独立战斗画面。

### 0.3 战斗架构校准

当前把以下内容分级处理：

**VERIFIED-HISTORICAL**

- 2003 年新浪 Q&A 明确区分“战斗画面/同一战斗场景”和“非战斗状态”；战斗画面左下角存在战斗指令/人物状态入口，非战斗状态使用四色球显示状态。
- 2003 年 17173 介绍明确写明：参战角色在 **行动槽蓄满后** 才能移动、攻击，行动后必须等待行动槽再次蓄满。
- 同期玩家资料出现“移动格子”“自己有行动回合的时候”“加快回合循环”等描述；官方新版介绍也描述为“进入回合制战斗画面后，在战局中即时战斗”。

因此项目不再把战斗实现成普通地图上的持续实时攻击。Web 原型结构改为：

`FIELD（非战斗自由移动） → BATTLE（独立状态/UI + 行动槽 + 战术移动/攻击） → FIELD（返回进入前位置）`

**UNVERIFIED / 待考据**

- 行动槽精确充能公式和毫秒值。
- 移动是否必然消耗完整一次行动、技能/物品的行动成本。
- 战斗场景背景与普通地图的具体资源映射、敌我 placement。
- 敌人 AI、行动槽速度、攻击范围、加入战斗、逃跑、胜利/失败判定等具体运行时规则。

当前 `battle-screen-policy-2` 只用于让结构先回到正确方向，不将临时参数伪装成原版事实。

### 0.4 已验证基线

| 项目 | 当前结论 | 证据/范围 |
| --- | --- | --- |
| 安装包 | 470,688,152 bytes | SHA-256 `c42f37b...d6cdae88` |
| 精确 7z payload | 469,089,543 bytes | SHA-256 `9beb6066...e996357` |
| 静态展开 | 22,885 个普通文件 / 2,699,237,296 bytes | 不执行原客户端 |
| ANI | 6,948 个，固定 1,236 bytes | 8×32 槽，只读取每行 active prefix |
| SPR | 行游程 RGB565 + bounds | span 首字段验证为相对 transparent skip |
| 双职业目标 | 20 阶段×5 动作，100 对 ANI/SPR | 严格解析与索引范围通过 |
| Body_ 方向 | `S,SW,W,NW,N,NE,E,SE` | B100/B109 真实帧人工/静态交叉检查 |
| B100 / B109 move | 5 / 4 | 静态 `ability.atr` 派生职业数据；在战斗原型中暂按最大移动格解释，语义仍需行为校准 |
| SGR | 92/92 可解析 | 0 错误 |
| 地图 0000 | 对练场，1536×768；IMF 47×47 | Web 已接入 |
| 地图 0001 | 布日古斯_外城，2240×1280；IMF 69×79 | Web 已接入 |
| MagicRes | 001/002/003/035/036/037/038 可顺序诊断播放 | FOCUS placement/timing/blend 仍 UNVERIFIED |
| Quest.lib | 21,205 bytes；15/15 members | 固定哈希，静态提取 |
| NPCScript | 39 NPC blocks / 125 active / 5 disabled | 声明数量全部对齐 |
| Quest0–9 | 10 files / 42 STEP / 179 dialogue commands | token 结构严格解析 |
| Tutorial | 42 TALK blocks / 173 text records | NAME/NEXT/NOTCLOSE/RGB/BR/DRAWTOP 等结构已解析 |
| HelpScript | 5 HELP / 14 STEP / 30 四整数记录 | 静态结构已解析 |
| Neohelp | 18 sections / 336 records | 第三字段非负引用均落在已有 section ID 内，语义尚不外推 |
| Prologue | 19 structural rows / 17 non-empty text rows | 静态结构已解析 |
| Tip | 27/27 文件严格闭合，3547 frames | `tools/convert/tip.py` + 全量验证 |
| NPC350.Tip | 图像 sprite library；3000×1125，50×(300×225)，10×5 | **不是** NPC placement/behavior 表 |
| Source-backed Web | 私有 pack 从固定 Quest.lib 生成 Quest/NPC JSON | 正文不提交 Git |
| 浏览器稳定性 | 30 分钟真实墙钟 soak 成功 | run `34969057806`，旧检查点 |
| SPR/方向修正回归 | synthetic + private-original 成功 | run `34971954768` |
| 战斗大结构 | field/battle 分离 + 行动槽概念有 2003 同期资料支持 | exact timing/scene binding 尚未验证 |

### 0.5 当前未完成

- 用户需要复测 **人工验证版 v3**：路线线条/方框是否消失、field→battle 是否符合记忆、行动槽门禁和战斗移动感觉是否接近原作。
- 当前 battle mode 已经是独立状态/UI，但仍暂时复用当前地图的平面背景；原版具体 battle scene 资源/placement 映射尚未恢复，不能伪造一个“看似战场”的背景冒充原版。
- 训练敌人、行动槽充能速度、伤害/AI、战斗移动消费规则仍为显式 `UNVERIFIED` 工程占位。
- Quest/NPC 与地图实体、坐标、触发点、条件分支、奖励、招募等原版运行时绑定尚未恢复。
- MagicRes/FOCUS 的原版 placement、blend、阶段衔接、timing；音效、死亡表现、前景遮挡仍需恢复。
- 完整背包/装备、成长/转职、正式任务链、更多地图流程仍未完成。
- 公开 Web 预览的访问控制/版权审查未完成，原版资源不得擅自公开部署。
- 尚未找到可信的公开原始游戏源码仓库；可继续搜索韩服/日服/私服实现，但任何第三方实现只作二级证据。
- 若静态证据无法恢复关键行为，M4 需要隔离、可回滚 Windows VM；普通环境与 CI 禁止执行原始 `NeoDark.exe` 或未知 DLL。

### 0.6 下一步

当前优先级：

1. 完成 `codex/battle-mode-fix` synthetic + fixed-hash private-original 浏览器回归，生成 v3 私有单 HTML 给用户复测。
2. 根据用户记忆继续校准 field→battle 过渡、行动槽、移动格、攻击/技能/逃跑等交互；历史资料能决定的直接收敛，不能决定的继续标 `UNVERIFIED`。
3. 静态追踪真实 battle scene / placement / encounter trigger 来源：Dlg/Tdg、Quest/NPC、地图数据、固定 `NeoDark.exe` 纯静态 xref。
4. 继续搜索可信公开原始源码或服务端实现；若无源码，不将资源解析脚本或第三方复刻误称为原作源码。
5. 同步继续 NPC/Quest dispatcher、音频、遮挡层和 MagicRes placement/timing 考据。

---

## 1. 项目目标

### 1.1 实施路线

**证据固化 → 资源转换 → Web 诊断 → Web 可玩切片 → 行为校准 → 核心系统 → 双职业完整化 → Web 发布 → 可选联网**

终端用户目标是打开现代浏览器即可体验；不要求安装原 Windows 客户端、Godot、Node、Python 或 VM。私人单文件 HTML 作为当前最快的人机联合验收载体。

### 1.2 MVP 验收

1. 浏览器启动并选择剑士/巫师。
2. 非战斗地图正常自由移动；通过可验证入口进入独立战斗画面/状态，战斗结束或逃跑后正确返回。
3. 战斗采用行动槽驱动的战术移动/攻击框架；精确规则未恢复时明确标注，禁止退回普通实时砍杀模型。
4. 待机、移动、方向、攻击、受击、施法、死亡表现；未知项明确标注。
5. 每职业普通攻击和至少 3 个代表技能；敌人、伤害、死亡、结算形成闭环。
6. NPC 对话、地图切换、基础背包和装备。
7. IndexedDB 存读档，JSON 导入导出作为可迁移备份。
8. Debug Panel 可检查角色、动作、方向、帧、bounds/anchor、地图坐标与通行数据；正常玩家画面默认不显示路线 polyline 和 bounds 方框。
9. 可重复构建、固定输入指纹、实际 Chromium 回归、真实墙钟稳定性验证和人工视觉/玩法复核。

仅有自动测试、按钮或占位训练逻辑，不足以宣布原版还原正确。

### 1.3 V1 验收

剑士 B100–B190、巫师 B109–B199 各十阶段，从初始阶段正常成长到最高阶段：外形、动作、属性、主要技能树、装备适配、升级/转职、代表任务、地图、战斗场景、行动机制、音频、设置和存档恢复。按职业矩阵逐项验收。

### 1.4 暂缓范围

第三职业、大规模全部原作内容；公网账号、充值、交易、运营后台；与未知第三方服务器互通；原生移动 App/主机；Windows-first 发行；未经授权公开散布原资源。PWA/桌面封装只作为未来发行选项。

---

## 2. 技术与资产边界

- 主运行时：Phaser + TypeScript；Vite 构建；HTML/CSS 调试 UI。
- Python >= 3.12：静态提取、ANI/SPR/SGR/MMF/SMF/IMF/Set.lib/Quest.lib/Tip 解析、转换和验证。
- 浏览器消费 PNG/JSON/适配音频；不执行原 EXE/DLL，不在前端存密钥。
- 当前锁定依赖以 `web/package-lock.json` 为准。
- GitHub 私有仓库只保存代码、文档、工具、结构、哈希、测试和少量合成样本。
- 原安装包、完整展开、大量派生美术/音频和原始对白保存在私有归档/短期 CI artifact，不进入 Git。
- `web/public/game-data/` 与 `game/generated/` 默认 gitignore。
- 改动先功能分支/PR；未获明确批准不合并 `main`。

---

## 3. 证据规则

`VERIFIED`：固定哈希、严格解析、全量验证、像素/帧检查、静态反汇编或隔离环境重复实验直接支持。

`VERIFIED-HISTORICAL`：来自同期官方/主流媒体/玩家操作资料，足以确认高层玩法或 UI 架构，但不等于拿到了内部源码。

`INFERRED`：有较强线索但语义仍需进一步证实，例如部分动作槽名称、兼容代码代表旧客户端行为。

`UNVERIFIED`：为推进原型暂用的设计替代，例如移速、动画毫秒值、行动槽毫秒数、训练敌人、M3 引导任务。

约束：

- 获得新证据后更新 `docs/evidence-ledger.md` 和回归测试。
- ANI raw timing 不自动当毫秒/FPS；`_05` 不统一当死亡。
- FOCUS 不套用 Body_ 八方向规则。
- 当前已确认的 Body_ raw direction row 只用于 B100/B109 等同类角色资源；若发现其他 layer family 不一致，单独记录。
- 锚点使用 SPR bounds；人物视觉连续性还需人工回归，不因数学边界合法就宣布正确。
- 正常玩家画面不绘制寻路 polyline；bounds/anchor 属于调试信息，默认关闭。
- field 与 battle 必须作为不同游戏状态建模；即使暂时复用同一平面背景，也不能把普通地图自由移动/攻击逻辑冒充原版战斗。
- Quest 文件 token 只按文件事实解释；无调用链/行为证据时，不外推其游戏语义。
- 私有原作正文只在构建时派生，不提交 Git。

---

## 4. 里程碑与门禁

| 里程碑 | 交付 | 状态 |
| --- | --- | --- |
| M0 基线 | 固定样本、哈希、静态拆包 | **G0 通过** |
| M1 资源 | 双职业、地图、技能、内容容器与格式转换 | **核心通过，持续收口** |
| M2 Web 诊断 | 地图0/1、B100/B109、逐帧/方向/碰撞/Debug/离线HTML | **工程通过；人物切片/方向已人工复测通过，正常叠层继续 v3 复核** |
| M3 Web 可玩切片 | field→battle→结算/返回→NPC/地图→存档；真实内容层；稳定性 | **战斗结构正在按行动槽模式重构；旧实时训练模型废弃** |
| M4 行为校准 | 输入、行动槽、移动格、时序、伤害、UI、任务触发 | 同期资料/静态考据中；动态 VM 未开始 |
| M5 核心系统 | 实体、战斗、技能、成长、任务、背包、存档迁移 | 未完成 |
| M6 双职业完整化 | 十阶段职业矩阵 | 未开始 |
| M7 Web 发布 | 内容、性能、兼容、访问控制、版权、回滚 | 未开始 |
| M8 可选联网 | 单机稳定后的独立权威服务端 | 暂缓 |

门禁：

- **G0：已通过。**
- **G1：核心通过。** 目标职业资源和至少一张地图可确定性转换。
- **G2-Web：工程自动化已通过；人工视觉为持续维护门禁。** SPR span、方向和正常画面调试叠层都已纳入人工 spot-check。
- **G3：尚不通过。** 旧的实时训练闭环不再算作原作战斗闭环；必须至少完成 field/battle 分离、行动槽战术交互、退出返回，并让用户人工确认大方向后再继续收口具体规则。
- **G4：**关键行为有可信证据或明确接受的正式替代。
- **G5：**双职业矩阵完成或批准例外；否则不称 V1。
- **G6：**公开发布前完成访问控制、版权、构建检查并取得发布授权。

---

## 5. 当前阶段任务

### M1 持续收口

维护 100 对目标 ANI/SPR、92 SGR、Set.lib/Quest.lib、27 Tip、地图与 MagicRes 全量回归。继续音频/UI/字体、资源索引和 MagicRes/FOCUS 语义考据。解析器遇到损坏输入必须显式报错，不静默修补。

### M2 人工诊断

离线单 HTML 是当前人工验证主载体。每次重大角色/地图/特效解析变化后至少验证：人物完整性、八方向朝向、动作帧顺序、脚底锚点、地图遮挡和技能 placement。Debug overlay 必须 opt-in；正常移动不显示路线 polyline 和 bounds 框。自动 E2E 负责“能运行”，人工检查负责“看起来/运动语义正确”，两者不能互相替代。

### M3 可玩切片

按原作大结构分成：

- **FIELD**：非战斗自由移动、NPC/任务/地图交互。
- **BATTLE**：独立状态/UI；行动槽 READY 后才允许一次战术移动或攻击/技能；移动格/攻击范围可视化等待进一步恢复。
- **RETURN**：胜负或逃跑后回到对应 field 状态/位置。

当前“进入战斗画面”按钮是研发入口，不声称是原版 encounter trigger。真实 battle scene、敌我 placement、行动槽参数、加入战斗、逃跑与胜利规则继续从资料/静态客户端恢复。

### M4 行为校准

优先继续纯静态考古和同期资料交叉验证。若必须动态运行原客户端，只允许一次性 Windows VM/快照，不使用真实密码、不挂载敏感目录、不绕过认证、不攻击第三方服务。

---

## 6. 质量与自动化

必须持续通过：

- Python parser tests（含 SPR relative-span、ANI、Quest、Tip 等）。
- TypeScript typecheck/unit tests/build。
- Chromium E2E 和单 HTML 离线测试。
- field/battle 状态转换、行动槽消费/恢复、战斗退出返回 field 的自动回归。
- fixed-hash private-original smoke：先校验安装包 SHA-256，再只做静态展开/转换。
- 真实资源重大检查点做 30 分钟 wall-clock soak；普通逻辑测试不能冒充。
- 人物/地图/战斗视觉语义重大变更必须安排人工 spot-check。

CI 不运行原安装器、`NeoDark.exe`、未知 DLL、兼容注入或 Frida 组件。

---

## 7. 文档职责

- `AGENTS.md`：项目执行/安全/分支控制。
- `Plan.md`：路线、门禁、实时状态。
- `Backlog.md`：具体任务。
- `docs/evidence-ledger.md`：事实证据。
- `docs/validation/`：自动与人工验收记录。
- `docs/decisions/`：重大架构决策。
- `manifests/`：固定输入/输出指纹。

---

## 8. 维护规则

1. 实质检查点完成时同步更新本文件状态和对应 Backlog。
2. 分开报告：本地成功、CI 成功、浏览器成功、人工视觉成功、历史玩法证据、原版运行时证据；禁止把其中一个替代另一个。
3. 不写无依据完成百分比。
4. ref 更新只做非 force 快进；不自动合并 `main`。
5. Windows VM 未就绪不阻止 Web、解析、测试和静态考古。
6. 真正阻塞时记录缺失输入/能力、已尝试方法和最小人工动作。
7. 对话结束后不得声称仍在后台开发；只有明确已启动的 CI/自动任务可以描述为运行中。

---

## 9. 变更记录

- 2026-09-14 v1.1：M0 基线与 Windows/Godot 路线。
- 2026-09-15 v2.0：用户批准 Web-first；Windows VM 仅用于原版行为考据。
- 2026-09-15 v2.1：建立 Web 诊断/训练、锁文件、CI、离线 HTML、哈希和存档。
- 2026-09-15 v2.2：关闭 G2-Web 工程门禁；加入地图0001、MagicRes 与最小 NPC→地图→任务→存档闭环。
- 2026-09-15 v2.3：PR #2/#3 合并 main；恢复 Quest.lib/NPCScript/Quest0–9，建立 source-backed 内容层。
- 2026-09-15 v2.4：完成 Tutorial/Help/Neohelp/Prologue 与 Tip 静态结构；30 分钟真实墙钟 soak 通过；首轮人工验收发现 SPR span 与方向映射问题并完成真实资源修复，新增人工视觉门禁。
- **2026-09-15 v2.5：用户确认 SPR/方向修复正确；第二轮人工验收移除正常画面路线线条、默认隐藏 bounds；依据 2003 同期资料纠正战斗架构为 field/battle 分离 + 行动槽驱动的半回合战术模式，废弃普通地图实时训练作为原作模型。**
