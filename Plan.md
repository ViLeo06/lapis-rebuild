# 《佣兵传说》复刻项目计划

> 文档版本：v1.1  
> 更新日期：2026-09-14  
> 项目定位：个人怀旧、研究与非商业复刻  
> 第一优先级职业：剑士、巫师

## 1. 结论与实施路线

本项目采用“证据固化 → 资源格式还原 → 离线可玩闭环 → 剑士/巫师完整化 → 内容扩充 → 可选联网”的路线。

第一阶段不直接追求完整 MMO，而是先做一个 Windows 离线可运行版本：玩家能进入地图、操控角色、战斗、释放技能、与 NPC 交互并保存进度。这个闭环稳定后，再逐步补齐原版职业阶段、技能、装备、地图和任务。联网账号、多人同步和服务端放在独立阶段，避免服务器逻辑缺失拖垮前期进度。

推荐技术基线：

- 游戏引擎：Godot 4.x，导出 Windows x64。
- 资源解析：Python 3.12；所有原始格式通过独立转换工具转为 PNG、JSON、WAV/OGG 等通用格式。
- 数据层：JSON/CSV 作为开发期中间格式；游戏运行时使用 Godot Resource 或打包后的只读数据。
- 版本管理：Git 管理代码、文档和转换脚本；大体积原始客户端与版权资源不直接进入公开仓库。
- 自动化验证：解析器单元测试、关键画面对比、战斗数值测试、Windows 构建冒烟测试。

### 1.1 存储与协作基线

- 唯一代码仓库：`https://github.com/ViLeo06/lapis-rebuild`（私有）。
- 大文件归档：Google Drive 私有目录 `lapis-rebuild-assets`。
- GitHub 保存代码、文档、解析工具、结构化数据、测试、清单和精选小样例。
- Google Drive 保存原始安装包、内层资源包、完整解压归档、批量解析产物和大体积预览。
- GitHub 清单通过稳定的 Drive 文件夹 ID、相对路径、来源、版本、大小和 SHA-256 关联归档；不记录密码、令牌或临时下载地址。
- 首次工程化变更使用 `codex/m0-baseline` 分支并通过 Pull Request 审核，不直接提交到 `main`。

## 2. 项目目标

### 2.1 最小可玩版本（MVP）

MVP 必须形成一个可验证的完整体验：

1. Windows 下启动并进入主界面。
2. 可选择剑士或巫师进入游戏。
3. 至少包含一张安全地图和一张战斗地图。
4. 支持原版风格的角色移动、朝向、待机、攻击、受击、施法和死亡表现。
5. 剑士和巫师各实现普通攻击及至少 3 个有代表性的技能。
6. 支持敌人、伤害、死亡、掉落或战斗结算。
7. 支持 NPC 对话、地图切换、基础背包和装备。
8. 支持本地存档和读档。
9. 形成可重复构建的 Windows 安装包或免安装包。

### 2.2 第一正式版本（V1）

在 MVP 基础上，V1 重点完成：

- 剑士 `B100–B190` 十阶段外形及对应动作资源。
- 巫师 `B109–B199` 十阶段外形及对应动作资源。
- 两条职业路线的等级、HP、MP、移动、攻击范围、职业系别和技能数据。
- 两个职业的主要技能树、装备适配和升级体验。
- 一组能体现原版循环的城镇、野外、战斗/副本地图与任务。
- 设置、音频、存档、异常恢复和 Windows 兼容性。

“完整职业”以能够从初始阶段正常成长到最高阶段为验收口径，不只要求立绘或动作资源齐全。

### 2.3 暂不纳入首版

- 原版全部职业、地图、任务和怪物。
- 公网账号体系、充值、交易和运营后台。
- 与非授权原服务端或第三方服务器互通。
- 移动端、网页端和主机平台。
- 公开分发原客户端或未经许可的原始美术、音乐资源。

## 3. 当前基线

已确认的成果：

- 官方站点：`https://figupaw.com`
- 已定位 2.2 客户端：`https://oss.figupaw.com/client-updates/YBCS-Online-Setup-2.2.exe`
- 安装包为 32 位 Windows PE/NSIS，自带 `_ybcs_payload.7z`，无需运行安装器即可静态提取。
- 安装包大小为 470,688,152 字节；已有 SHA-256 前后片段记录，正式归档时需重新计算并保存完整值。
- 内层资源展开约 2.699 GB，共约 22,885 个文件。
- 客户端是“2003 年 NeoDark 原生客户端 + 2026 兼容运行时”的混合包。
- `Set.lib` 中已找到职业、属性和技能相关表。
- 已确认剑士职业编号路线 `100/110/.../190`，巫师路线 `109/119/.../199`。
- `.spr` 已确认是“帧边界 + RGB565 行游程”格式，并已成功解出可视帧。
- `.ani` 已观察到固定长度的动作/帧索引结构，但动作语义尚未完整映射。

第一项实际工作不是重新研究全部内容，而是将以上样本、哈希、脚本、预览和结论固化为可复现的项目基线。

## 4. 里程碑总览

| 里程碑 | 目标 | 主要交付物 | 预计工作量 | 状态 |
| --- | --- | --- | ---: | --- |
| M0 基线归档 | 样本、哈希、工具和结论可复现 | 样本清单、哈希清单、拆包说明、现有解析器 | 2–4 人日 | 进行中 |
| M1 资源解码 | 目标职业、动作、特效和地图可批量转换 | `.spr/.ani/.sgr/MagicRes` 转换器、资源索引 | 8–15 人日 | 部分完成 |
| M2 行为基准 | 明确原版操作、战斗和 UI 规则 | 行为录像、状态表、交互与数值规则文档 | 5–10 人日 | 未开始 |
| M3 可玩切片 | 完成一张地图上的移动和战斗闭环 | Windows Demo、剑士/巫师基础角色、敌人 | 12–20 人日 | 未开始 |
| M4 核心系统 | 补齐成长、技能、装备、NPC、存档 | 可扩展核心模块及自动化测试 | 15–25 人日 | 未开始 |
| M5 双职业完整化 | 剑士、巫师可完整成长和战斗 | 十阶段资源、技能树、职业验收报告 | 15–25 人日 | 未开始 |
| M6 内容与发布 | 形成稳定的离线怀旧版本 | 地图/任务样板、安装包、使用说明 | 10–18 人日 | 未开始 |
| M7 可选联网 | 在离线版稳定后增加多人能力 | 独立服务端、账号与同步协议 | 另行评估 | 暂缓 |

以单人开发配合 AI 工具估算，首个可玩切片约 3–5 周，V1 约 3–5 个月。实际进度主要取决于 `.ani`、地图格式、技能逻辑和服务端缺失程度，时间只用于排期，不作为承诺。

## 5. 分阶段执行计划

### 阶段 0：项目基线与证据固化

任务：

- 重新下载 2.2 安装包，记录来源、时间、大小、完整 SHA-256。
- 仅静态提取 `_ybcs_payload.7z`；不直接运行登录器、注入组件和未知 DLL。
- 保存原始文件清单、目录树、文件类型、大小和哈希。
- 恢复并纳入现有 `.spr` 解析脚本、剑士/巫师预览图和客户端分析报告。
- 将所有研究结论区分为“已验证”“高概率推断”“待验证”。
- 建立可重复的一键拆包/索引流程。

交付物：

- `docs/client-analysis.md`
- `docs/evidence-ledger.md`
- `manifests/client-2.2.sha256`
- `manifests/client-files.csv`
- `tools/extract/`
- `tools/convert/spr.py`

验收标准：换一台干净开发机，只使用安装包和项目脚本，能够得到相同的文件清单与首批角色预览。

### 阶段 1：资源格式与数据层还原

任务：

1. 完成 `.spr` 批量解码，确认透明色、帧中心、偏移、调色和异常帧处理。
2. 解析 `.ani`，建立“动作 → 方向 → 帧序列 → 播放时长 → 循环规则”映射；方向数量以证据为准，不预设为八方向。
3. 解析 `MagicRes`，恢复巫师法术和其他技能特效的帧序列、锚点及混合方式。
4. 解析 `.sgr` 地图资源，识别图块、阻挡、出生点、传送点、NPC/怪物点位及层级。
5. 整理 `Set.lib` 和其他表文件，输出职业、技能、装备、怪物、地图、任务的结构化数据。
6. 盘点音频、字体、界面和文本编码；统一转换但保留原始编号。
7. 生成资源索引，建立原始文件、转换文件、游戏对象之间的可追溯关系。

验收标准：

- 剑士、巫师全部目标阶段都能批量导出透明背景帧和动作预览。
- 至少一张目标地图能正确渲染并产生可用碰撞/通行数据。
- 至少 3 个剑士技能、3 个巫师技能的视觉资源与数据记录可定位。
- 转换器对损坏或未知文件给出明确错误，不静默生成错误资源。

### 阶段 2：原版行为基准

资源只能还原“长什么样”，行为基准决定“玩起来是否像”。如需运行原客户端，应在一次性 Windows 虚拟机中执行，先做快照并限制文件与账号权限，不使用用户的常用系统和真实密码。

需要记录：

- 分辨率、坐标系、镜头、地图滚动、UI 布局和输入方式。
- 行走路径、转向、移动速度、碰撞、攻击范围和施法距离。
- 普通攻击、技能前摇/命中/后摇、伤害显示、受击与死亡。
- 回合、行动点或时间推进机制；不能仅凭记忆假设战斗规则。
- 职业升级、转职、装备、属性变化和技能学习。
- NPC、商店、任务、地图切换和战斗进入/退出流程。
- 网络请求只做被动观察和协议边界识别，不尝试绕过认证或攻击服务器。

交付物：`docs/behavior-spec/`，每条规则附录像时间点、截图、数据表或二进制证据。

验收标准：MVP 需要实现的每项关键行为，都能追溯到一条明确证据或一条标注过的设计性替代方案。

### 阶段 3：垂直可玩切片

按最短闭环搭建 Godot 工程：

- 主菜单和本地角色选择。
- 一张地图的渲染、镜头、碰撞和寻路。
- 剑士、巫师的待机、移动、攻击、受击、施法、死亡动画状态机。
- 一个近战敌人和一个远程/施法敌人。
- 普通攻击、目标选择、范围判定、伤害、MP 消耗和冷却/行动消耗。
- 每个职业先接入 3 个代表性技能。
- 一名 NPC、一段对话、一次地图切换和一次战斗结算。
- 自动存档、手动存档和读档。

验收标准：从启动游戏到完成一次战斗并保存进度，整个流程不依赖开发控制台或手工改文件；连续运行 30 分钟无阻断性错误。

### 阶段 4：核心系统工程化

将可玩切片中验证过的功能拆为可扩展模块：

- 实体与属性：角色、怪物、NPC、装备、Buff/Debuff。
- 战斗：目标、范围、命中、伤害、治疗、状态、死亡、经验和掉落。
- 技能：数据驱动的施法条件、资源消耗、作用范围、效果和动画事件。
- 地图：图块、碰撞、寻路、传送、事件触发与刷新点。
- 成长：等级、职业阶段、技能学习、装备限制和属性成长。
- 内容：对话、商店、任务、奖励和条件。
- 系统：输入映射、设置、音频、存档迁移、日志与错误报告。

关键约束：技能和职业规则不得散落为大量硬编码；同一套系统应能通过数据扩展到更多职业。

### 阶段 5：剑士与巫师完整化

剑士重点：

- 十阶段外形、动作、武器与装备表现。
- 近战距离、移动、单体/范围攻击和防御类能力。
- 每个阶段的成长、技能解锁和手感差异。

巫师重点：

- 十阶段外形、动作、法术特效和施法锚点。
- MP、施法距离、范围、弹道/落点和状态效果。
- 不同阶段技能解锁、属性成长和战斗定位。

验收采用“职业矩阵”：每个阶段逐项检查外观、全部动作、属性、可装备物、可学习技能、伤害/消耗、升级与存档恢复。任何缺失项必须标记为“待考据”或“设计替代”，不能用相似资源悄悄代替。

### 阶段 6：内容、质量与 Windows 发布

任务：

- 选取一组最能代表原版体验的城镇、野外和战斗/副本地图。
- 制作从接任务、探索、战斗到奖励的短流程。
- 补齐 HUD、背包、装备、技能、对话、设置和存档界面。
- 优化资源加载、内存、路径寻找和大量特效性能。
- 测试 Windows 10/11、常见分辨率、窗口/全屏、中文路径和无管理员权限环境。
- 生成版本号、更新日志、已知问题、操作说明和可回滚构建。

发布门禁：首次进入、完整战斗、存档读档、职业升级和地图切换均通过；高优先级缺陷清零；原始版权资源不进入公开分发渠道。

### 阶段 7：可选联网版本

只有离线 V1 稳定后才启动：

- 单独设计权威服务端，不依赖未知第三方服务。
- 先实现局域网或私有邀请制联机，再考虑公网部署。
- 服务端负责账号、角色、地图状态、战斗判定和持久化。
- 客户端只提交意图，重要数值由服务端验证。
- 加入版本兼容、断线恢复、日志、限流、备份和数据迁移。

联网阶段需要重新进行隐私、安全、成本和运营边界评估。

## 6. 建议目录结构

```text
mercenary-legend-remake/
├── Plan.md
├── README.md
├── docs/
│   ├── client-analysis.md
│   ├── evidence-ledger.md
│   ├── behavior-spec/
│   └── decisions/
├── manifests/
├── tools/
│   ├── extract/
│   ├── convert/
│   └── validate/
├── assets_raw/          # 本地保存，不进入公开仓库
├── assets_derived/      # 转换产物，可按授权范围管理
├── data/
│   ├── classes/
│   ├── skills/
│   ├── maps/
│   └── items/
├── game/                # Godot 工程
├── tests/
│   ├── parsers/
│   ├── gameplay/
│   └── golden/
└── builds/
```

## 7. 验证与质量策略

- 解析器：每种格式保存若干“黄金样本”，比较帧数、尺寸、偏移、像素哈希和异常处理。
- 动画：自动生成动作表和 GIF/序列图，人工核对方向、帧序和锚点抖动。
- 地图：原始预览与引擎渲染截图对比；检查边缘、遮挡、阻挡和传送点。
- 数值：对职业成长、技能消耗、范围、伤害和状态编写确定性测试。
- 回归：每个里程碑固定一套从启动到存档的自动/半自动冒烟流程。
- 构建：每次发布保留版本号、提交号、数据版本、构建日志和 SHA-256。

## 8. 风险与边界

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 原版资源版权不明 | 无法公开发布或商业化 | 默认仅用于个人研究；代码与版权资源分离；公开版本换用自制或获授权资源 |
| 客户端或兼容运行时不可信 | 系统、账号与数据安全风险 | 优先静态分析；动态运行只在快照虚拟机中进行；不用常用账号和密码 |
| 服务端逻辑缺失 | 无法准确还原战斗、任务和经济系统 | 以行为录像和数据表交叉验证；缺失规则明确标注为设计替代 |
| `.ani/.sgr/MagicRes` 解析复杂 | 阻塞动画、地图和技能 | 先针对剑士、巫师和一张地图做最小解析，再扩展通用性 |
| 中文编码、旧格式与路径兼容 | 文本乱码、文件加载失败 | 原始字节留档；自动识别/显式声明编码；测试中文路径 |
| 范围持续膨胀 | 长期没有可玩版本 | 以门禁控制范围；MVP 前不扩展第三职业和公网服务端 |
| 记忆与实际规则不一致 | 做成“印象中的游戏” | 所有关键规则写入证据台账，区分事实、推断和替代设计 |
| 临时文件或网站资源消失 | 研究成果不可复现 | 保存合法样本、完整哈希、来源记录、转换工具和派生索引 |

## 9. 决策门禁

- G0：基线可复现。未通过前，不大规模制作派生资源。
- G1：目标职业资源与一张地图可稳定转换。未通过前，不进入完整游戏工程。
- G2：关键行为有证据。无法考据的部分必须由项目负责人明确接受替代设计。
- G3：可玩切片通过 30 分钟稳定性测试。未通过前，不扩充职业和地图。
- G4：剑士、巫师职业矩阵全部完成或明确标记例外。未通过前，不称为 V1。
- G5：Windows 发布门禁、版权边界和打包检查通过后，才生成对外交付物。

## 10. 下一步：首个两周冲刺

### 第 1 周：固化与解码

1. 完善现有项目目录、证据台账和决策记录。
2. 重新归档客户端并生成完整哈希、文件清单和拆包脚本。
3. 恢复 `.spr` 解码器，批量导出剑士/巫师全部阶段并生成验证图。
4. 集中突破 `.ani`，输出第一版动作、方向和帧序映射。
5. 盘点 `MagicRes`、`.sgr`、音频、文本和 UI 文件，确定解析优先级。

### 第 2 周：地图与行为样本

1. 选定一张最小测试地图，完成静态渲染和初步通行数据解析。
2. 各选 3 个剑士/巫师技能，建立资源、数据与行为证据链。
3. 在隔离环境中采集移动、攻击、施法、受击和地图交互基准。
4. 创建 Godot 空工程，验证角色帧、地图和坐标系能正确组合。
5. 召开一次范围复核，只决定 MVP 地图、敌人和 6 个技能，不提前扩展内容。

两周冲刺完成的判定：能够在 Godot 测试场景中显示一张地图，并正确播放剑士、巫师至少一组移动和战斗动画；所有输入资源均可追溯到原文件与转换脚本。

## 11. 计划维护规则

- 每完成一个里程碑，更新状态、实际工作量、未解决问题和下一里程碑范围。
- 关键技术或范围变化写入 `docs/decisions/`，记录原因、选项与后果。
- 新发现的原版行为先进入证据台账，再进入实现任务。
- 每两周只设一个主要可验证目标，避免同时推进资源、玩法、联网和内容四条主线。
- 本文件是路线基线；具体任务应拆入 `Backlog.md`，但不得绕过本文件的门禁与范围边界。
