# AGENTS.md

本文件定义所有参与 `lapis-rebuild` 项目的 AI Agent、Codex/Work 任务、自动化脚本和人工协作者必须遵守的项目级规则。

> 项目：`ViLeo06/lapis-rebuild`  
> 项目定位：个人怀旧、研究与非商业复刻  
> 当前主路线：**Web-first**  
> 第一优先级：剑士、巫师  
> 主计划：[`Plan.md`](./Plan.md)

---

## 1. 总原则

1. 以 `Plan.md` 为项目路线、门禁和实时状态的单一主计划。
2. 默认持续推进，不因缺少 Windows VM、原客户端动态行为证据或非关键未知项而停止 Web 原型、解析工具、测试、CI 和数据层工作。
3. 优先做可验证、可回归、可复现的工程，不以“看起来像”代替证据。
4. 原版事实、推断和临时设计必须严格分开。
5. 不为了技术复杂度而复杂化；能用成熟、简单方案解决时，不自行建立不必要的大型框架。
6. 第一阶段目标不是完整 MMO，而是高质量、可调试的浏览器可玩闭环。

---

## 2. 当前技术路线

### 2.1 主运行时

默认使用：

- Vite
- TypeScript
- Phaser
- HTML/CSS 调试与辅助 UI

Web 工程位于：

```text
web/
```

除非 `docs/decisions/` 中有新的明确架构决策，否则：

- 不把 Godot 恢复为默认主运行时。
- 不优先开发 Windows 原生客户端。
- 不要求终端用户安装 Windows 运行环境。

现有 `game/` Godot 工程仅保留作历史验证、参考或实验用途。

### 2.2 资源与工具链

Python 3.12 继续负责：

- 安装包静态拆解
- 原始格式解析
- 资源转换
- 数据清洗
- 资源索引
- 验证与回归测试

目标转换格式优先：

- PNG
- JSON
- OGG/WAV
- 必要的 Web 友好二进制格式

浏览器运行时不应直接依赖旧客户端专有格式，除非为了开发期诊断且有明确理由。

---

## 3. 项目状态同步规则

任何完成了“实质性项目进展”的 Agent，都必须检查是否需要同步更新 `Plan.md`。

以下变化必须更新 `Plan.md`：

- 里程碑或门禁状态变化
- 新的已验证格式结论
- 主要架构变化
- 新的关键阻塞项
- 关键阻塞解除
- 当前下一目标变化
- 影响 MVP/V1 范围的需求变化

更新位置优先为：

```text
Plan.md -> 0. 当前状态
```

必要时同时更新具体里程碑章节。

禁止使用无法验证的模糊百分比，例如：

```text
项目已完成 80%
```

优先使用：

```text
100/100 个目标 ANI/SPR 配对通过验证
92/92 个 SGR 可解析
地图 0 与参考渲染逐像素一致
```

---

## 4. 证据等级

所有关于原版客户端、资源、玩法和行为的结论必须归为以下之一：

### VERIFIED

有直接、可复现证据支持，例如：

- 完整哈希
- 严格二进制解析
- 静态源码/反编译证据
- 全量样本验证
- 像素级对比
- 隔离环境中的可重复行为实验

### INFERRED

高概率推断，但语义尚未完全证实，例如：

- `_00` 很可能是 idle
- 方向槽 0 很可能对应 south
- 某效果资源可能是前景/后景层

### UNVERIFIED

为了推进开发采用的临时值或替代设计，例如：

- 临时移动速度
- 临时动画毫秒数
- 尚未考据的控制方式

规则：

- 不得把 `INFERRED` 写成 `VERIFIED`。
- 不得把 `UNVERIFIED` 隐藏成原版事实。
- 临时参数应集中配置，不要散落在代码中。
- 获得新证据后，应更新 `docs/evidence-ledger.md` 并补回归测试。

---

## 5. 原客户端安全边界

默认只进行静态分析。

以下内容禁止在普通开发环境、宿主机、CI、GitHub Actions 中执行：

- 原始安装器
- `NeoDark.exe`
- 未知 DLL
- 兼容注入组件
- Frida/注入相关组件
- 任何来源不明的原始可执行文件

如必须运行原客户端做行为考据：

1. 使用一次性/可回滚 Windows VM。
2. 先做快照。
3. 不使用用户真实账号和密码。
4. 不挂载敏感个人目录。
5. 默认只做被动观察。
6. 不绕过认证。
7. 不攻击、不扫描、不破坏第三方服务器。

动态考据是提高复刻精度的手段，不是 Web 开发的前置条件。

---

## 6. Git 与分支规则

仓库：

```text
ViLeo06/lapis-rebuild
```

规则：

1. 不直接向 `main` 提交。
2. 所有工程变更在功能分支完成。
3. 通过 Pull Request 集成。
4. 未经项目负责人明确批准，不合并到 `main`。
5. 当前阶段可继续使用活跃分支 `codex/m0-ani` 作为集成分支，直到该 PR 完成收口或明确切换分支。
6. 提交应小而清晰，提交信息说明“做了什么”，避免大而模糊的混合提交。
7. 修改同一文件时，先读取当前版本，避免覆盖他人更新。

建议提交类型：

```text
feat: ...
fix: ...
docs: ...
test: ...
refactor: ...
chore: ...
```

---

## 7. Google Drive 与大文件规则

大文件私有归档根目录：

```text
lapis-rebuild-assets
```

约定：

```text
00_inbox/
10_original/
20_extracted/
30_parsed/
40_previews/
90_quarantine/
```

GitHub 只保存：

- 代码
- 文档
- 解析脚本
- 结构化数据
- 测试
- 哈希
- 清单
- 少量合法/合成黄金样本

默认不要把以下内容提交进 Git：

- 原始安装包
- 完整解压客户端
- 批量原始美术/音频
- 大体积派生资源
- 未授权版权资源

Git 中引用 Drive 资源时，优先记录：

- 稳定文件夹 ID
- 相对路径
- 来源
- 版本
- 文件大小
- SHA-256

禁止记录：

- 密码
- API Token
- Cookie
- 临时签名下载地址
- 个人敏感信息

---

## 8. Web-first 开发规则

### 8.1 第一优先级

任何 Web 原型优先保证：

1. 正确加载地图。
2. 正确加载 B100/B109。
3. 动作槽和方向可检查。
4. 帧序可逐帧查看。
5. bounds/anchor 可视化。
6. 地图网格和通行数据可视化。
7. Debug Panel 可读取底层状态。

先“可检查”，再“好看”。

### 8.2 Debug Panel 是核心研发工具

早期不得为了 UI 简洁删除 Debug Panel。

至少应支持：

- 角色 ID
- 动作槽
- 方向槽
- 当前帧
- ANI raw timing
- 像素坐标
- 地图逻辑坐标（已验证时）
- IMF/collision raw value
- walkable
- resource/tile ID（可可靠获得时）
- anchor/bounds
- VERIFIED / INFERRED / UNVERIFIED 标签

### 8.3 不提前优化

在 M2/G2-Web 通过前，不优先投入：

- 大型状态管理框架
- 复杂 ECS
- 微服务
- 服务端账号体系
- 多人同步
- 第三职业
- 大规模 UI 美化
- 高级 shader/粒子优化

---

## 9. 当前已知事实，禁止回退

以下结论已被项目验证，除非新证据明确推翻，否则不得重新用旧假设覆盖：

### 客户端

- 2.2 安装包大小：`470,688,152` 字节。
- 安装包 SHA-256：`c42f37b06f27a6ee0b14e6fea6129cf89956a3e1c7a37c1172a28577f6cdae88`。
- 精确 7z payload SHA-256：`9beb606655d2553c03e80d7eda36a48c135976a3812ce5432d3b2af23e996357`。
- 静态解包得到 22,885 个普通文件，约 2.699 GB。

### SPR

- `uint32 frame_count`
- 每帧 bounds 为 `<4i>`：left/top/right/bottom
- right/bottom 为 exclusive
- 后续为 RGB565 行游程数据
- 未覆盖像素透明

### ANI

- 所有已观察 `.ani` 为 1,236 字节。
- 当前样本 `layer_count=1`。
- 方向表为 `uint32[8][32]`。
- 只使用每方向前 `frames_per_direction` 个有效槽。
- 后续槽位可能含 stale data，禁止将其当有效帧索引。
- raw timing 单位仍未验证。

### 目标职业

剑士：

```text
100,110,120,130,140,150,160,170,180,190
```

巫师：

```text
109,119,129,139,149,159,169,179,189,199
```

目标 20 个阶段 × `_00/_01/_02/_03/_05` 共 100 对 ANI/SPR 已通过严格解析与索引范围验证。

### SGR/地图

- 92/92 SGR 已可解析。
- 地图 0“对练场”独立渲染与参考实现逐像素一致。
- MMF 中 selector bits `(packed >> 23) & 0x3f` 是直接 `resource_id`，不是资源表索引。
- 不得假设 `screen_to_cell(cell_to_screen(...))` 是直接恒等映射；当前坐标空间仍需进一步校准。

### Set.lib

- `Set.lib` 位于 `client/NRes/Set.lib`。
- 已恢复目录加密与 PKWARE DCL blast 解压链。
- 12 个成员可确定性提取。

### MagicRes

- `FOCUS` 类型不能直接套用 `Body_` 的八方向帧索引语义。
- 在完成更多证据前，只可做诊断播放，不得把其方向/索引解释固化为原版事实。

---

## 10. 测试要求

### 10.1 Python

修改解析器后至少运行相关：

- synthetic unit tests
- 剑士/巫师目标资源验证
- SGR 全量解析
- Set.lib 关键成员校验
- 地图生成/结构校验

若完整私有资源集在当前环境不可用：

- 必须运行仓库内可运行测试。
- 明确写出哪些全量验证未执行。
- 不得伪称测试通过。

### 10.2 Web

修改 Web 代码后至少保证：

```text
install
TypeScript typecheck
unit test（存在时）
production build
```

核心渲染和数据转换应尽量可测试，不要把所有逻辑塞入 Phaser Scene 生命周期函数。

### 10.3 CI

GitHub Actions 至少覆盖：

- Python tests
- 数据一致性检查
- Web typecheck/test/build

CI 不得运行原 Windows 客户端。

---

## 11. 文档规则

关键发现应进入正确位置：

```text
Plan.md
```

路线、里程碑、实时状态、门禁。

```text
docs/evidence-ledger.md
```

证据和事实等级。

```text
docs/client-analysis.md
```

客户端结构和静态分析。

```text
docs/decisions/
```

重大架构决策及原因。

```text
Backlog.md
```

具体待办，不改变 Plan 门禁。

禁止只在聊天记录里留下关键项目知识而不落库。

---

## 12. 版权与发布边界

本项目当前默认用于：

- 个人怀旧
- 技术研究
- 非商业复刻

未经额外确认：

- 不公开分发原始客户端。
- 不公开分发未经许可的原版美术、音频和大规模派生素材。
- 不将私人 Drive 资源无条件部署到公网。
- 不宣布商业授权状态。

公开 Web 部署前必须单独检查：

1. 资源版权。
2. 访问范围。
3. 仓库可见性。
4. 是否包含原版大体积素材。
5. 是否需要替换为自制/授权素材。

---

## 13. Agent 自主推进与停止条件

默认允许 Agent 自主执行低风险、可逆的项目工作，例如：

- 读取仓库
- 修改功能分支
- 添加代码和文档
- 添加测试
- 建立 CI
- 生成私有派生数据
- 更新 Plan/Backlog/evidence ledger
- 创建或更新 Draft PR 内容

以下情况必须停止并请求项目负责人确认：

- 合并到 `main`
- 公开发布或把私有版权资源部署到公网
- 删除/覆盖不可恢复数据
- 修改仓库/账号权限
- 付费行为
- 需要在非隔离环境执行原客户端/未知二进制
- 出现多个会显著改变游戏设计的替代方案，且证据无法决定

以下情况**不构成停止理由**：

- 尚未准备 Windows VM
- Godot 不可运行
- 某些原版 timing/行为未考据
- 部分原始资源尚未同步到 Drive
- 某些非 MVP 资源格式尚未完全理解

这些情况应使用 `UNVERIFIED`/Backlog 继续推进 Web 主线。

---

## 14. 每次任务结束前的检查清单

Agent 在完成一次实质性工作后，应检查：

- [ ] 是否遵守 Web-first 路线？
- [ ] 是否新增了无依据的原版事实？
- [ ] 是否运行了相关测试？
- [ ] 是否需要更新 `docs/evidence-ledger.md`？
- [ ] 是否需要更新 `Plan.md -> 0. 当前状态`？
- [ ] 是否需要更新 `Backlog.md`？
- [ ] 是否把大体积/版权资源错误提交进 Git？
- [ ] 是否误执行了原客户端或未知二进制？
- [ ] 是否需要创建/更新 PR？
- [ ] 是否越过了必须由用户确认的边界？

只要没有触发真实停止条件，就继续推进下一个最接近当前门禁的任务。
