# Web validation / 2026-09-15

## Actual results so far

- Run34933950368: original and synthetic each11/12. The combined80-case matrix timed out at60s. It was split, not removed.
- Run34935118627: all80 action/direction combinations passed. Overall20/22 each; two remaining failures were investigated.
- Offline failure: global replacement of `</body>` inserted license HTML into a JavaScript string inside the bundle. Fixed by inserting at the final document terminator only. Added regression test and checked both generated inline scripts with `node --check`.
- Training failure:800ms wall-clock wait did not guarantee a700ms simulation cooldown had elapsed on a slow runner. Test now observes the read-only cooldown state; it does not mutate gameplay or bypass conditions. Movement completion also waits for position change before queue-empty.
- Added six item texts with preserved source rows andSHA-256 from Set.lib/itemtbl.atr. Equip restrictions, starting ownership andbonuses are explicitly UNVERIFIED training policy.
- Local34Web tests,11Python tests,typecheck/build pass. Expanded24 browser tests and30-minute real-time diagnostic soak await this commit's CI.

Do not mark G2,fullM3 orV1 completed solely from these local results. The soak is a diagnostic animation/overlay stability test, not a substitute for full gameplay or Windows behavior calibration.
