# M0 收口状态

> 分支：`codex/m0-ani`  
> 更新日期：2026-09-15

## 当前结论

M0 的工程门禁已经基本满足，可以继续 M1，不再让“是否已经把整个 2.7 GB 展开目录同步到 Drive”阻塞资源逆向与原型开发。

## 已完成

- [x] 从 Google Drive 中的 18 个分卷重组 `YBCS-Online-Setup-2.2.exe`
- [x] 重组后大小：470,688,152 字节
- [x] 重组后 SHA-256：`c42f37b06f27a6ee0b14e6fea6129cf89956a3e1c7a37c1172a28577f6cdae88`
- [x] 仅静态定位并 carving 内嵌 7z，不运行安装器
- [x] 通过 7z Signature Header 精确确定 payload 边界，不把 NSIS trailer 误算进 7z
- [x] payload 大小：469,089,543 字节
- [x] payload SHA-256：`9beb606655d2553c03e80d7eda36a48c135976a3812ce5432d3b2af23e996357`
- [x] 完整静态展开：22,885 个普通文件，2,699,237,296 字节
- [x] 生成完整 `client-files.csv`
- [x] `.spr` 格式重新实样验证并工程化
- [x] `.ani` 核心字节结构突破：全量 6,948 个文件统计
- [x] 剑士/巫师 20 阶段 × 5 action slot，共 100 对 `.ani/.spr` 严格验证，0 错误
- [x] 建立客户端分析、证据台账与格式说明
- [x] 建立静态 carving、manifest、ANI、SPR、角色配对验证脚本与基础单元测试

## 归档状态

原始 18 个安装包分卷已进入 Drive `10_original`，这是可复现输入的最重要部分。

当前 ChatGPT 运行环境的 Drive 连接器不能把本地沙箱中生成的超大目录树直接作为上传文件引用，所以：

- `20_extracted` 的 2.7 GB 全量镜像尚未在本轮直接同步；
- 这属于归档层面的尾项，不再视为工程阻塞；
- 原始输入、完整哈希、carving 算法、提取脚本、全量文件清单均已固定，换一台环境可以重复获得相同结果。

## G0 判断

**G0：通过。**

理由：

1. 原始输入已持久化且重新验证；
2. 内嵌 payload 边界与哈希已重新验证；
3. 完整展开规模已重新获得；
4. 全量文件清单已生成；
5. `.spr/.ani` 解析已形成可测试代码；
6. 目标职业资源已通过自动交叉验证。

Drive `20_extracted` 全量镜像保留为非阻塞归档任务，不再延迟 M1 `.sgr/MagicRes/Set.lib` 与 Godot 原型准备。

## 下一步

按 `Plan.md` 继续：

1. 批量生成剑士/巫师 20 阶段的动作结构索引与预览；
2. 解析 `MagicRes` 的 ANI/SPR 关系；
3. 选择一张最小 `.sgr` 地图，突破静态地图格式；
4. 将 `Set.lib` 的职业/技能相关表转为结构化数据；
5. 选定剑士 3 技能、巫师 3 技能并建立资源—数据证据链；
6. G1 条件满足后创建 Godot 最小测试工程。
