# Tools

本目录用于保存可复现的静态提取、格式转换、索引和验证工具。

计划子目录：

- `extract/`：NSIS、内层归档和客户端目录提取。
- `convert/`：`.spr`、`.ani`、`.sgr`、`MagicRes` 和表文件转换。
- `validate/`：哈希、文件清单、黄金样本和输出一致性检查。

工具不得默认执行原客户端、登录器、注入组件或未知 DLL。任何动态分析脚本都必须明确标注隔离环境要求。

