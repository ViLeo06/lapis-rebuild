import type {Point} from './viewport-state.ts';
export type EnemyMotion=Readonly<{id:string;from:Point;to:Point;elapsedMs:number;durationMs:number}>;
function point(value:Point,label:string){if(!Number.isFinite(value.x)||!Number.isFinite(value.y))throw new RangeError(label+' must be finite');return{x:value.x,y:value.y};}
export function createEnemyMotion(id:string,from:Point,to:Point,durationMs:number):EnemyMotion{if(!id)throw new Error('enemy motion id is required');if(!Number.isFinite(durationMs)||durationMs<=0)throw new RangeError('enemy motion duration must be positive');return Object.freeze({id,from:Object.freeze(point(from,'from')),to:Object.freeze(point(to,'to')),elapsedMs:0,durationMs});}
export function advanceEnemyMotion(motion:EnemyMotion,deltaMs:number){const elapsedMs=Math.min(motion.durationMs,motion.elapsedMs+Math.max(0,Number.isFinite(deltaMs)?deltaMs:0)),linear=elapsedMs/motion.durationMs,t=linear*linear*(3-2*linear),position={x:motion.from.x+(motion.to.x-motion.from.x)*t,y:motion.from.y+(motion.to.y-motion.from.y)*t},done=elapsedMs>=motion.durationMs;return{motion:Object.freeze({...motion,elapsedMs}),position:done?{...motion.to}:position,progress:linear,done};}
